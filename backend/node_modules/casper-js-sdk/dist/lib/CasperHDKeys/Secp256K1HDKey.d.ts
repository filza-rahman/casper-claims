import { CasperHDKey } from './CasperHDKey';
import { Secp256K1 } from '../Keys';
/**
 * Casper HD Key for Secp256K1.
 *
 * Examples
 * ```ts
 *  const mn = Secp256K1HDKey.newMnemonic();
 *  const hdKey = Secp256K1HDKey.fromMnemonic(mn);
 *  const key0 = hdKey.deriveChild(0);
 * ```
 */
export declare class Secp256K1HDKey extends CasperHDKey<Secp256K1> {
    private hdKey;
    constructor(seed: Uint8Array);
    static new(): Secp256K1HDKey;
    static fromMnemonic(mnemonic: string): Secp256K1HDKey;
    publicKey(): Uint8Array;
    privateKey(): Uint8Array;
    derive(path: string): Secp256K1;
    sign(msg: Uint8Array): Uint8Array;
    verify(signature: Uint8Array, msg: Uint8Array): boolean;
}
